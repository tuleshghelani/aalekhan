import { AutoTypeDirective } from './auto-type.directive';

describe('AutoTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTypeDirective();
    expect(directive).toBeTruthy();
  });
});
